<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper 
	PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
	
<mapper namespace="mapper.product">

	<resultMap id="proResult" type="productVO">
		<result property="productNum" column="productNum" />
        <result property="productName" column="productName" />
        <result property="productPrice" column="productPrice" />
        <result property="category" column="category" />
        <result property="subcategory" column="subcategory" />
        <result property="productImage" column="productImage" />
        <result property="productManufacturer" column="productManufacturer" />
        <result property="productOrigin" column="productOrigin" />
        <result property="productContentImage" column="productContentImage" />
        <result property="option1name" column="option1name" />
        <result property="option2name" column="option2name" />
        <result property="option1price" column="option1price" />
        <result property="option2price" column="option2price" /> 
        <result property="option1value" column="option1value" />
        <result property="option2value" column="option2value" /> 
        <result property="deliverycharge" column="deliverycharge" /> 
	</resultMap>


	<!-- 상품 조회 -->

	<select id="selectAllProductList" resultMap="proResult"
		parameterType="java.util.Map">
	
		<if test='subsort == "x"'>
			select * from product where category = #{sort}
		</if>
		<if test='subsort != "x"'>
			select * from product where category = #{sort} and subCategory = #{subsort}
		</if>
		

	</select>
	
	
   <!-- 관리자 상품 조회 -->
	<select id="admin_selectAllProductList" resultMap="proResult">
	<![CDATA[
		select * from product order by category;
	]]>
	</select>
	
	
	
	<!-- 상품추가 -->
	<insert id="insertProduct"  parameterType="java.util.Map">
		<![CDATA[ 
			insert into simple.product(productNum, productName, productPrice, category, subcategory, productImage,productManufacturer,productOrigin,productContentImage) 
			values(#{productNum},#{productName},#{productPrice},#{category},#{subcategory},#{productImage},#{productManufacturer},#{productOrigin},#{productContentImage})
		
		]]>	
	</insert>
	
	
	
	<!-- 상품수정 -->
			<!-- into simple.productoption(productNum, option1, option2) 
			values(#{productNum},#{option1},#{option2})
		
		
			into simple.productdetail(productNum, productContentImage) 
			values(#{productNum},#{productContentImage}); -->
	<update id="updateProduct"  parameterType="java.util.Map">
			update product
			set productName = #{productName},	 
			productPrice = #{productPrice},
			category = #{category},	 
			subcategory = #{subcategory},
			productManufacturer = #{productManufacturer},
			productOrigin =#{productOrigin}
			 <if test="productImage!='' and productImage!=null">
			  , productImage=#{productImage},productContentImage=#{productContentImage},
			</if>
			where productNum=#{productNum}
	</update>
	<!-- 상품삭제 -->
	<delete id="deleteProduct" parameterType="java.util.Map">
		<![CDATA[ 
			delete from simple.product 
			where productNum = #{productNum};
		]]>
	</delete>
	
	<select id="selectProduct" resultType="productVO" parameterType="string"> 
	<![CDATA[
			
			select *
			from product
			where productNum = #{productNum};
		
	]]>
	</select>
	
	
	<select id="selectOptionList1" resultMap="proResult" parameterType="string"> 
	<![CDATA[
			
			select option1name, option1value, option1price from detailoption where productNum = #{productNum} group by option1name, option1value, option1price;
		
	]]>
	</select>
	
	<select id="selectOptionList2" resultMap="proResult" parameterType="string"> 
	<![CDATA[
			
			select option2name, option2value, option2price from detailoption where productNum = #{productNum} group by option2name, option2value, option2price;
		
	]]>
	</select>
	
	<!-- 
	<select id="selectOptionlist3" resultMap="proResult" parameterType="string"> 
	<![CDATA[
			
			elect option3name, option3value, option3price from detailoption where productNum = #{productNum} group by option3name, option3value, option3price;
		
	]]>
	</select>
	-->
	
	<select id="selectProductBySearchWord" resultMap="proResult"   parameterType="String"  >
     <![CDATA[
				select * from product
		    where productName like CONCAT('%', #{searchWord}, '%')
		    order by category;
		]]>
	</select><!-- 검색창에 입력한 단어를 가져와 그 단어가 포함된 상품 제목 조회 -->
	
	<select id="selectKeywordSearch" resultType="String"   parameterType="String"  >
	    <![CDATA[
        select productName from product 
        where         
        productName like CONCAT('%', #{keyword}, '%')
        order by category;
		]]>
	</select> <!-- 검색창에 입력한 키워드를 가져와 그 키워드가 포함된 상품 제목 조회 -->

</mapper>